---
- hosts: controller
  name: Install k3s on controller node
  become: yes
  gather_facts: yes
  roles: 
    - role: ../roles/packages
      tags: packages
    - role: ../roles/controller
      tags: controller
